# CZ人生（NBT）经济模型与合约逻辑分析

> 版本：基于链上快照 2026-08-10  
> 分析范围：[NBTToken.sol](file:///e:/dapp/%E8%B4%A8%E6%8A%BC%E9%A1%B9%E7%9B%AE/contracts/NBTToken.sol)、[NBTStakingBank.sol](file:///e:/dapp/%E8%B4%A8%E6%8A%BC%E9%A1%B9%E7%9B%AE/contracts/NBTStakingBank.sol) 及 BSC 主网实际部署数据

---

## 1. 项目与合约概览

### 1.1 合约清单

| 合约 | 文件 | 主网地址 |
|---|---|---|
| 代币合约 | [NBTToken.sol](file:///e:/dapp/%E8%B4%A8%E6%8A%BC%E9%A1%B9%E7%9B%AE/contracts/NBTToken.sol) | `0xD0F2A86C7EbCeE887F5bFB86771f994CD142bD04` |
| 质押/奖励合约 | [NBTStakingBank.sol](file:///e:/dapp/%E8%B4%A8%E6%8A%BC%E9%A1%B9%E7%9B%AE/contracts/NBTStakingBank.sol) | `0x903fcce5d67648FBE6Dccc9806e3bd7D303380fD` |

### 1.2 关键链上身份

| 角色 | 地址 | 说明 |
|---|---|---|
| `StakingBank` owner | `0x9B0c7994F4289Aa247d06A96C4447EB36c13CfBb` | `.env.mainnet` 私钥对应地址，最高权限 |
| `StakingBank` operator | `0xFa42EeeC0830A2FdCCEC9479f562FFf92319Cf26` | 曾开启第 1 期月度发放 |
| 手续费接收 A | `0xfd682CbCb678ce5D273Eb778B946F6a4d8f1e8Ed` | BNB 交互费 50% 归此处 |
| 手续费接收 B | `0x5A378b61193ac2ce07cE816893C080804504a2f0` | BNB 交互费 50% 归此处 |

### 1.3 首要发现：源码与链上实际代币不一致

- 仓库源码 [NBTToken.sol](file:///e:/dapp/%E8%B4%A8%E6%8A%BC%E9%A1%B9%E7%9B%AE/contracts/NBTToken.sol) 设计的代币名为 **NBT**，计划总量 **2 亿**，并带可配置的买卖手续费。
- 链上实际部署的代币合约 `0xD0F2...BD04` 名为 **CZ人生**，总量 **2100 万亿**，且没有源码中 `buyFee`/`sellFee`/`feeReceiver` 等函数。
- 这意味着：仓库里的代币源码 **并不是当前主网代币的实现**。后续任何基于 NBTToken.sol 的假设（手续费、总供应量、权限结构）都要与链上 CZ人生 区分开。

---

## 2. 代币合约分析

### 2.1 源码层面的 NBTToken

[NBTToken.sol](file:///e:/dapp/%E8%B4%A8%E6%8A%BC%E9%A1%B9%E7%9B%AE/contracts/NBTToken.sol) 是一个在标准 ERC20 基础上叠加交易税的代币：

- **权限模型**：合约没有 `owner`，只有构造时传入的 `feeReceiver`。`feeReceiver`、`buyFee`、`sellFee`、`isPair` 都在构造后不可更改。
- **交易税逻辑**：
  - 当 `from` 是配对地址时，按 `buyFee` 扣税（买入税）。
  - 当 `to` 是配对地址时，按 `sellFee` 扣税（卖出税）。
  - 税费直接加到 `feeReceiver` 余额。
  - `isExcludedFromFee` 的地址免手续费，初始默认排除 `deployer` 和 `feeReceiver`。
- **扩展功能**：任何人可以 `burn` 自己的代币。

**潜在问题**：

1. 配对地址一旦确定后不可增删。如果后续迁移流动性或新增交易对，旧配置会失效。
2. 没有暂停、黑名单、mint 等应急功能，方向上是“一次性”代币。
3. 由于 `feeReceiver` 不可改，若该地址丢失私钥，手续费将永久沉淀。

### 2.2 链上实际的 CZ人生 代币

链上查询结果：

| 参数 | 数值 |
|---|---|
| 名称 / 符号 | CZ人生 / CZ人生 |
| 小数位 | 18 |
| 总供应量 | 2,100,000,000,000,000（2100 万亿） |
| `owner()` | `0x000000000000000000000000000000000000dEaD`（已弃用/黑洞） |
| 买卖手续费 | 无 |

它是一个超大总量的标准 ERC20，没有交易税，也没有可升级的权限。项目方无法通过合约层面 mint 或冻结。

---

## 3. StakingBank 合约核心逻辑

[NBTStakingBank.sol](file:///e:/dapp/%E8%B4%A8%E6%8A%BC%E9%A1%B9%E7%9B%AE/contracts/NBTStakingBank.sol) 是整个经济系统的核心，承担了“质押、推荐、排名、奖励发放”四大职能。

### 3.1 角色与权限

| 角色 | 权限 |
|---|---|
| `owner` | 转让所有权、设置 operator、设置交互费、设置奖励参数、recoverWrongToken、设置 stakeValueRate、设置 minReferralStakeValue |
| `operator` | 暂停/恢复合约、开启/分配月度发放 |
| 普通用户 | 质押、提取、设置推荐人、领取奖励、复利再投 |

### 3.2 质押与提取

#### 质押 `stake(uint256 amount, address referrer)`

- 每次质押需支付 `interactionFee`（当前为 BNB）。
- 质押金额经 `transferFrom` 进入合约，合约按实际到账数量记录。
- 质押后进入 **15 天锁定期**。
- 同一地址最多同时存在 **50 笔活跃质押**。
- 质押时若用户还没有推荐人，可一次性绑定；若已绑定则传入的 `referrer` 必须与之一致。
- **月度发放未最终化时禁止质押**（`noActiveRelease` 修饰符）。

#### 提取 `withdraw(uint256 stakeId)`

- 仅可提取锁定期已满的活跃质押。
- 每次提取也需支付 BNB 交互费。
- 提取后该笔质押的 `scoreValue` 会从推荐人的节点分数中扣除，从而可能降低其排名。
- **月度发放未最终化时禁止提取**。

### 3.3 推荐关系

- 每个用户只能绑定一次推荐人，绑定后不可更改。
- 推荐关系限制深度 20 层，且禁止循环推荐。
- 绑定推荐人也会触发 `interactionFee`。

### 3.4 节点分数与排名

- 推荐人的节点分数 `referralStakeVolume` 等于其直接推荐人的**累计有效质押值**之和。
- `stakeValueRate` 默认 1，所以分数≈被推荐人质押的代币数量。
- 节点排名是动态维护的有序数组 `_rankedNodes`，按分数降序、同分时地址小者优先。
- 当推荐人的分数变为 0 时，会从排名中移除。

### 3.5 邀请奖励

- 当被推荐人完成一笔质押，且该笔质押的 `scoreValue >= minReferralStakeValue`（当前 100 CZ）时，推荐人获得一笔 `inviteReward`（当前 **100 万 CZ**）。
- 邀请奖励先进入 `lockedInviteRewards`，锁定期同样为 15 天。
- 锁定期满后，调用 `claimNodeRewards` 或 `compoundNodeRewards` 时会自动解锁并转入 `pendingInviteRewards`。
- 只有在 `_rewardReserveAvailable()` 足够时，才会发放新的邀请奖励。

### 3.6 月度释放池（Rank Reward）

#### 开启 `openMonthlyRelease(uint256 amount)`

- 只能由 `owner` 或 `operator` 调用。
- 调用者必须先将对应数量的 CZ 授权给合约，合约通过 `transferFrom` 从调用者钱包扣款。
- 开启后生成新的一期 `MonthlyRelease`，记录奖金池金额、当前上榜节点总数、`nextRank=1`。
- **重要：月度发放的奖金来自调用者钱包，而不是合约里已存在的余额。**

#### 分配 `allocateMonthlyRelease(uint256 maxCount)`

- 按当前排名顺序依次给上榜节点分配奖励。
- 分配算法把节点分成 4 档：
  - 第 1–10 名：权重 5000
  - 第 11–50 名：权重 3000
  - 第 51–100 名：权重 1500
  - 第 100 名以后：权重 500
- 当期有效权重之和为 `activeWeight`，某一档总奖金 = `amount * 该档权重 / activeWeight`。
- 档内平分：每个节点获得 `档总奖金 / 该档节点数`。
- 最后一名的奖金会兜底为 `amount - allocatedAmount`，防止舍入误差。
- 当 `nextRank > totalNodes` 时，本期结束并标记 `finalized=true`。

### 3.7 奖励储备计算

合约通过 `_rewardReserveAvailable()` 判断能否继续发放邀请奖励：

```text
reserve = rewardToken.balanceOf(this)
        - (totalInviteRewardsAccrued - totalInviteRewardsClaimed)
        - (totalRankDistributed - totalRankClaimed)
        - activeReleaseUnallocated
if stakingToken == rewardToken:
        reserve -= totalStaked
```

含义：合约余额中，扣掉已质押本金、已分配但待领取的奖励、本期未分配月度奖金后，剩余部分才是可用于新增邀请奖励的储备。

### 3.8 交互费

- 当前 `interactionFeeToken` 为 `address(0)`，表示用 **BNB** 支付。
- 每笔交互（质押、提取、设置推荐人、领取奖励、复利）固定支付 `interactionFee` = `0.000701754385964912 BNB`（约 $0.4）。
- BNB 分成两份 50/50 转给 `feeReceiverA` 和 `feeReceiverB`，多付部分退回用户。
- 如果设置成 ERC20 手续费代币，则逻辑改为从用户地址扣代币。

---

## 4. 经济模型拆解

### 4.1 资金流向

```
用户钱包
  │ 质押/复利        ┌──────────────┐
  └───────────────▶│ StakingBank  │
                   │   合约       │
                   └──────────────┘
                          │
          ┌───────────────┼───────────────┐
          ▼               ▼               ▼
      邀请奖励        排行奖励          本金提取
      (给用户)        (给用户)          (还给用户)
          │               │
          └───────────────┘
                  │
          月度发放池充值
          (owner/operator 钱包转入)
```

### 4.2 关键经济参数（链上实际值）

| 参数 | 当前值 | 说明 |
|---|---|---|
| 单份邀请奖励 | 1,000,000 CZ | 每成功推荐一个有效质押用户 |
| 有效推荐门槛 | 100 CZ | 被推荐人单笔质押值 ≥ 100 CZ |
| 质押锁定期 | 15 天 | 期满才可提取 |
| 邀请奖励锁定期 | 15 天 | 期满才可领取 |
| 最大活跃质押数 | 50 笔 | 单个地址 |
| 交互费 | ~0.000702 BNB | 每次交互 |
| 月度发放第 1 期 | 10,000,000 CZ | 已分配完毕并结束 |

### 4.3 奖励杠杆分析

当前参数下，推荐奖励与被推荐质押金额严重不成比例：

- 被推荐人只需质押 **100 CZ**，推荐人即可获得 **1,000,000 CZ**。
